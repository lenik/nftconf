# Second context: management NIC address scope.
# Inherits table/priority; uses the address/interface set in nftconf.conf.

shield on

allow incoming tcp 9100      # mgmt-only host port on gw
allow incoming tcp 22        # optional ssh on mgmt
