# Outgoing (OUTPUT) policy for the docker lab.
#
# Included from the generated runtime conf *before* any `interface` line so
# these rules do not pick up an oifname match. Destination is the CIDR on
# each allow/deny outgoing line.
#
# Function-cover tests overwrite this file and restore it afterwards.
# Keep the file present even when empty of policy.
